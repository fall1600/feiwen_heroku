<div class="navbar-collapse collapse">
  <ul class="nav navbar-nav">
    <%= nav_li '關於本站', pages_home_path %>
    <%= nav_li '首頁', posts_path %>
    <%= nav_li '標籤列表', tags_path %>
    <%#= nav_li '新增標籤', new_tag_path %>
    <%#= nav_li '新增文章', new_post_path %>
    <%#= nav_li '關於', about_path %>
    <%#= nav_li '樂透', math_path %>
    <%#= nav_li '表單', form_path %>
  </ul>

  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
          <%= current_user.name %>
          <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
          <li><%= link_to "我的費文 :D", user_posts_path(current_user) %></li>
          <li><%= link_to "個人資料", user_path(current_user) %></li>
          <li role="separator" class="divider"></li>
          <li><%= link_to "登出", session_path, method: :delete %></li>
        </ul>
      </li>

      <%#= link_to current_user.name, user_posts_path(current_user) %> 
      <%#= link_to "登出", session_path, method: :delete %>
      <%#= link_to '個人資料', user_path(current_user) %>
    <% else %>
      <%= nav_li "登入", pages_login_path %>
    <% end %>
  </ul>
</div>
